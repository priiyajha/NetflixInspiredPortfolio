# Environment Configuration for Portfolio App

# Application Environment
NODE_ENV=production

# Server Configuration
PORT=5000

# Session Configuration (REQUIRED for production)
# Generate a secure random string for production deployment
SESSION_SECRET=your-secure-session-secret-here

# Frontend URL (for CORS configuration)
FRONTEND_URL=https://your-app-domain.com

# Database Configuration (if using PostgreSQL)
DATABASE_URL=postgres://username:password@host:port/database

# Keep-alive Service Configuration (optional)
RENDER_EXTERNAL_URL=https://your-app.onrender.com
RENDER_SERVICE_NAME=your-service-name